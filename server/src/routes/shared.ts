
import {Router} from "express";

const router: Router = Router();
//Authenticated sharing
// router.post("documents/:id/share");
// router.delete("documents/:id/share")//remove share link
// router.get("documents/:id/share")//Get current share status(for what?)
// //Public sharing
// router.get("shared/:token");//token?
export default router;