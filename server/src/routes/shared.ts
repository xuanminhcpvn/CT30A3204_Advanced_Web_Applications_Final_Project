
import {Router} from "express";

const router: Router = Router();
//Authenticated sharing
// router.post("files/:id/share");
// router.delete("files/:id/share")//remove share link
// router.get("files/:id/share")//Get current share status(for what?)
// //Public sharing
// router.get("shared/:token");//token?
export default router;